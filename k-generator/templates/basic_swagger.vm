package ${class.packageName};
${class.imports.toString("import ",";")}
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;

/**
 * @description  $!{class.comment}
 * @author ${author}
 * @since ${date}
 **/
@ApiModel(value="${class.name}对象", description="$!{class.comment}")
public class ${class.name} ${class.getSuperClassName("extends ")} ${class.interfaces.toString("implements ")}{
#foreach($field in ${class.fields})
    @ApiModelProperty(value = "${field.comment}")
    private ${field.type} ${field.name};
#end
}
