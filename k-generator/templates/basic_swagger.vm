package ${class.packageName};
#foreach($pkg in ${class.importPackages})
import ${pkg};
#end
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;

/**
 * @description  $!{class.remarks}
 * @author ${author}
 * @since ${date}
 **/
@ApiModel(value="${class.name}对象", description="$!{module.table.remarks}")
public class ${class.name} ${class.superClassName} ${class.interfaces}{

#foreach($field in ${module.table.columns})
    @ApiModelProperty(value = "${field.remarks}")
    private ${field.clazz.getSimpleName()} ${field.name};
#end

}
